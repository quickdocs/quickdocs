<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <link rel="shortcut icon" href="favicon.ico" />
  <title><% @var title %></title>
  <link rel="stylesheet" type="text/css" href="/css/LigatureSymbols/style.css" />
  <link rel="stylesheet" type="text/css" href="/css/base.css" />
  <link rel="stylesheet" type="text/css" href="/css/footer.css" />
  <link rel="stylesheet" type="text/css" href="/css/index.css" />
</head>
<body class="page-index" onLoad="document['search-form'].q.focus()">
<div id="container">
  <div id="content">
    <header>
      <h1 class="logo large-logo">ClackDoc</h1>
    </header>

    <form class="search-form" name="search-form" method="get" action="/search">
      <input type="text" name="q" value="<% @var query %>" placeholder="Search for projects" />
      <input type="submit" class="lsf" value="search" />
    </form>

    <% @with count %>
    <p class="distribution-counts"><%= (format nil "~:D" (getf env :releases)) %> projects / <%= (format nil "~:D" (getf env :systems)) %> systems</p>
    <% @endwith %>

    <% @include footer %>
  </div>
  <div id="background-blue"></div>
  <div id="background-yellow"></div>
</div>
</body>
</html>
